[package]
name = "tuto"
version = "0.1.0"
edition = "2024"

[dependencies]
creusot-std = { version = "0.14.0-dev", features = ["sc-drf"] }

[features]
solutions = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(creusot)'] }
