[package]
name = "tuto"
version = "0.1.0"
edition = "2024"

[dependencies]
creusot-contracts = "0.8.0"

[features]
solutions = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(creusot)'] }

[patch.crates-io]
creusot-contracts = { path = "creusot/creusot-contracts" }